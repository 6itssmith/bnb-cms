# Base URL of the backend API (see BACKEND_SETUP.md). Leave blank to demo the
# frontend stand-alone — payment buttons will show a graceful "connect backend" message.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
